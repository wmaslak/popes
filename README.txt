The order in the naming of the files indicates order in which they were created and in 
the same time allow one to follow the proces from beginning to end.

Despite that, the recommended order is to 

#### start with the 4_summary notebook ########

and refer to the
other notebooks whenever the reader is interested in the coding details.

Contents of the project:
> helper_fins.py : contains functions used in the process that were written by the author
> ldavis: contains interactive visualizations of the LDA models html 
> models: contain ALL models that were fitted during the modelling proces (so a lot of them)
> pickles: contains python objects that were save to be accesible between notebooks
> txts: contains raw text files that are analyzed grouped in folders
> csv: contains some helper dataframes
